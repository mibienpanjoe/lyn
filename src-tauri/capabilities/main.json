{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Library window: chronology, search, settings, and optional speech model commands. No capture-session mutation surface.",
  "windows": ["main"],
  "permissions": [
    "core:event:default",
    "library-commands"
  ]
}
